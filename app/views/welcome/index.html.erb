<div class="row">
  <div class="col-md-12">

    <div class="jumbotron">
      <h1>Welcome to WOD Tracker</h1>
      <p> This is a test application. </p>
      <p><a class="btn btn-primary btn-lg" href='<%= new_user_registration_path %>' role="button">Sign Up</a></p>

    </div>
    <div>
      <%= image_tag("homepage.jpg") %>
    </div>


    <% if user_signed_in? %>
      <div>Signed in as <%= current_user.first_name %> (<%= current_user.email %>)</div>
    <% end %>

  </div>
</div>